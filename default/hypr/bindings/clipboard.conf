# Clipboard bindings

# Clipboard history (using wl-clipboard)
# Note: For full clipboard manager functionality, consider installing cliphist
bind = SUPER, V, exec, cliphist list | walker --dmenu | cliphist decode | wl-copy

# Copy/paste shortcuts are handled by applications, not Hyprland
