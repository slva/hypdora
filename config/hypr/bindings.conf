# User keybindings - Add your custom bindings here
# These override the defaults from ~/.local/share/omarchy-fedora/default/hypr/bindings/

# Application bindings
bind = SUPER, RETURN, exec, alacritty
bind = SUPER SHIFT, F, exec, nautilus --new-window
bind = SUPER SHIFT, B, exec, firefox
bind = SUPER SHIFT, B, exec, firefox --private-window

# Overwrite existing bindings example:
# unbind = SUPER, SPACE
# bind = SUPER, SPACE, Custom launcher, exec, walker
