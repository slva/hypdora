# User keybindings - Add your custom bindings here
# These override the defaults from ~/.local/share/omarchy-fedora/default/hypr/bindings/

# Application bindings
bindd = SUPER, RETURN, Terminal, exec, alacritty
bindd = SUPER SHIFT, F, File manager, exec, nautilus --new-window
bindd = SUPER SHIFT, B, Browser, exec, firefox
bindd = SUPER SHIFT, B, Browser (private), exec, firefox --private-window

# Overwrite existing bindings example:
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Custom launcher, exec, wofi --show drun
